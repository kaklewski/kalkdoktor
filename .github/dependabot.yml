version: 2
updates:
    - package-ecosystem: 'npm'
      directory: '/'
      target-branch: 'develop'
      schedule:
          interval: 'weekly'
      ignore:
          - dependency-name: '*'
            update-types:
                - 'version-update:semver-major'
